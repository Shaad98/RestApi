spring.application.name=demo
server.port=9090
# spring.datasource.url=jdbc:postgresql://localhost:5432/rest_proj
# spring.datasource.username=postgres 
# spring.datasource.driver-class-name=org.postgresql.Driver

# spring.jpa.show-sql=true
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# MongoDB connection URI (replace with your actual host, port, and db)
spring.data.mongodb.uri=mongodb://localhost:27017/rest_proj


############################# MongoDB Atlas #####################
# db_password you get from security--> database access --> edit -->copy password-->update click
# click on connect on free cluster then driver select language you get url to connect
# In security --> network access provide your ip for connect for every ip write 0.0.0.0/0 

# spring.data.mongodb.uri=mongodb+srv://shaadbangi98:<db_password>@cluster0.vzomq.mongodb.net/<db_name>?retryWrites=true&w=majority&appName=Cluster0

###############################################################################

# Or you can specify these separately:
# spring.data.mongodb.host=localhost
# spring.data.mongodb.port=27017
# spring.data.mongodb.database=your_database_name

# Optional: MongoDB credentials if authentication is enabled
# spring.data.mongodb.username=your_username
# spring.data.mongodb.password=your_password

# Enable MongoDB logging (optional)
# logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG